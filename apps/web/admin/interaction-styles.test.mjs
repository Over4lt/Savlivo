import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
const css=fs.readFileSync(new URL('./admin.css',import.meta.url),'utf8');
const rule=selector=>{const start=css.lastIndexOf('\n'+selector+'{')+1;assert(start>0,'Missing selector '+selector);return css.slice(start+selector.length+1,css.indexOf('}',start));};
test('shared accent border covers buttons, workflow buttons, navigation and selects',()=>{assert.match(rule(':is(button,.admin-view-nav a,select)'),/border-color:var\(--control-outline\)/);assert.match(css,/--control-outline: #87cba8/);});
test('disclosures and toggle controls have scoped interactive affordances',()=>{assert.match(rule('summary'),/inset 0 0 0 1px var\(--control-outline\)/);assert.match(rule('.ops-research-builder .ops-tool-switch:has(input:enabled)'),/border-color:var\(--control-outline\)/);assert.match(rule('input:is([type="checkbox"],[type="radio"]):not(.ops-tool-switch-input)'),/outline:1px solid/);});
test('only session revocation opts into danger semantics',()=>{assert.match(rule('#revoke'),/--control-outline:var\(--control-danger\)/);assert.equal((css.match(/--control-outline:var\(--control-danger\)/g)??[]).length,1);});
test('hover and press require enabled controls without geometry changes',()=>{for(const state of ['hover','active']){const text=rule(':is(button:enabled,.admin-view-nav a:not([aria-disabled="true"]),select:enabled):'+state);assert(!/padding|margin|border-width|transform/.test(text));}assert(!css.includes('button:hover {'));});
test('disabled controls retain a muted shape and no pressed shadow',()=>{const text=rule(':is(button:disabled:not(.ops-action-busy),select:disabled,.admin-view-nav a[aria-disabled="true"])');assert.match(text,/border-color:var\(--control-muted\)/);assert.match(text,/cursor:not-allowed;box-shadow:none/);});
test('keyboard focus is separate and outside the normal border',()=>{assert.match(rule(':is(button,.admin-view-nav a,summary,select,input[type="checkbox"],input[type="radio"]):focus-visible'),/outline:3px solid var\(--control-focus\);outline-offset:5px/);});
test('selected navigation and toggled buttons have a stronger inset edge',()=>{assert.match(rule(':is(button[aria-pressed="true"],.admin-view-nav a[aria-current="page"])'),/inset 0 0 0 1px/);assert.match(rule('.ops-research-builder .ops-tool-switch:has(input:checked:enabled)'),/inset/);});
test('ordinary links, status pills and containers are not action selectors',()=>{const shared=css.slice(css.indexOf('/* Interactive affordances only:'));assert(!/(?:^|\n)(?:a|label|details|\.ops-state|\.ops-lifecycle li|\.ops-result-card)\s*\{/.test(shared));});
test('processing uses the shared accent and reduced motion disables animation',()=>{assert.match(css,/button\.ops-action-busy\{opacity:1;border-color:var\(--control-outline\);outline:2px solid var\(--control-outline\)/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)\{button\.ops-action-busy\{animation:none;outline:2px solid var\(--control-outline\)/);assert.match(css,/@keyframes ops-action-pulse.*4px var\(--control-outline\)/);});
